# Core (required to build / run the models)
torch>=2.0
transformers>=4.30
peft>=0.6
einops>=0.6
numpy>=1.24

# Optional: SeisBench integration for S2S_dpk
# seisbench>=0.10
# obspy>=1.4

# Optional: preprocessing examples / visualization
# pandas>=2.0
# matplotlib>=3.7
